# Project: `07-properties-file-usage`

Keep Database connection related configuration information outside Java code.

Instead, use .properties file to keep them.

Parse the file using `java.util.Properties`.
